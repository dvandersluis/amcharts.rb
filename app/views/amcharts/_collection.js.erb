<% collection.each.with_index do |obj, index| -%>
  <%= render layout: 'amcharts/object.js.erb', object: obj, locals: local_assigns.merge(var: "#{name}#{index}") do |obj, var| %>
    <%= yield(obj, var) rescue nil %>
  <% end %>
<% end %>